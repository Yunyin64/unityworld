# <BookName> 策划数据表

> summary: <≤150字简介，说明该 Book 配置什么>

## Book 概述
- **Book 名**：`<BookName>`（Excel 文件 `<BookName>.xlsm`）
- **所属目录**：`<ddtconfig 中的 Dir>`
- **用途**：<一句话说明这个 Book 配置什么>
- **填表时机**：<什么时候需要新增/修改行>
- **C/S 分发**：<仅客户端/仅服务端/双端>
- **ID 规则**：<ID 编码规则，如 BuffId = BuffCls*100+Level；无特殊规则写"自增">

## SubBooks

<若无 SubBooks，写"无 SubBooks，全部数据在主 Book 中">

| SubBook | 说明 | ID 分区 |
|---------|------|---------|
| <SubBook1> | <说明> | <ID范围> |

> ID 分区来自 SubBooks.lua 中 `_subbooks_name2idparition`。

## Sheet 总览

| 来源 | Sheet / SubSheet | 说明 | 子文档 |
|------|-----------------|------|--------|
| Book | <SheetName> | <结合 Sheet 内容给出有意义的描述> | [<link>](<name>.<sheet>.designdata.md) |
| SubBook: <name> | <SubSheet>@<SheetName> | <说明> | 同上 |
| Book | <SheetName2> | <说明> | [<link>](<name>.<sheet2>.designdata.md) |

> "来源"列标注该 Sheet/SubSheet 属于 Book 还是某个 SubBook。说明列需结合 Sheet 内容给出有意义的描述，不要只重复 Sheet 名。

## 联动与影响

### 被依赖（谁引用了我）

| 引用方 | 引用方列 | 本 Book 列 | 关系 | 说明 |
|--------|---------|-----------|------|------|
| <引用方全局表名> | <列名> | <Sheet名>.<列名> | <外键/引用/反向/配对> | <说明> |

> 列名需加 Sheet 前缀（如 `Buff.ID`、`Appear.icon`），明确列属于哪个 Sheet。若无被依赖，写"无外部表引用本 Book"。

### 我依赖（我引用了谁）

| 本 Book 列 | 被引用方 | 被引用列 | 关系 | 说明 |
|-----------|---------|---------|------|------|
| <Sheet名>.<列名> | <被引用全局表名> | <列名> | <外键/引用/反向/配对> | <说明> |

> 列名需加 Sheet 前缀。本 Book 内部 Sheet 间引用也要列出（如 `Appear.FxID → Fx_TargetFx.ID`）。若无跨表依赖，写"无跨表依赖"。

### 影响范围

<一句话总结：修改本表 ID/核心列会影响哪些系统的行为>

## 新增行操作指引

新增一个 <配置类型> 时，需按以下步骤操作：

1. 在本表/分表中新增一行，分配 ID（ID 区间：<XXXX-XXXX>）
2. 在 <关联表1> 中新增对应的 <配置项>（搜索关键字：`<keyword>`）
3. 在 <关联表2> 中注册 <配置项>（搜索关键字：`<keyword>`）
4. 配置 <其他必要步骤>
5. 验证：<游戏内验证步骤>

---

# 以下为 profile 子文档模板

# <BookName> <子文档标题>

> summary: <≤150字简介>

## Sheet: <SheetName1>

### 列定义

> **规则**：列语义以代码逻辑为准，不直接复制表头 Row1 注释。偏差加 `⚠️ 表头描述偏差：表头写"..."实际用于...`；缺失加 `⚠️ 表头描述缺失`；模糊加 `⚠️ 表头描述模糊`。

| 列名 | 类型 | C/S | 语义 | 约束 | 默认值 | 依赖 |
|------|------|-----|------|------|--------|------|
| <列1> | <类型> | <c/s/cs> | <代码验证后的语义> | <约束> | <默认值> | <依赖> |
| <列2> | <类型> | <c/s/cs> | <语义> ⚠️ 表头描述偏差：表头写"XXX"实际用于YYY | <约束> | <默认值> | <依赖> |

### 枚举值：<<枚举列名>>

| 值 | 含义 | 对应行为/Handler | 关联子表 |
|----|------|-----------------|---------|
| <值1> | <含义1> | <Handler/行为> | <子表1> |
| <值2> | <含义2> | <Handler/行为> | <子表2> |

> 仅为取值为有限枚举的列生成此表。无枚举约束的列跳过。

### 高价值行示例

**示例1：<场景描述>**

| 列 | 值 |
|----|-----|
| ID | <值> |
| <核心列1> | <值> |
| <核心列2> | <值> |

**示例2：<场景描述>**

| 列 | 值 |
|----|-----|
| ID | <值> |
| <核心列1> | <值> |
| <核心列2> | <值> |

> 每个 Sheet 选 2-3 个代表性行，覆盖不同枚举值/配置场景。仅列关键列，不需列全。

---

## Sheet: <SheetName2>

### 列定义

| 列名 | 类型 | C/S | 语义 | 约束 | 默认值 | 依赖 |
|------|------|-----|------|------|--------|------|
| ... | ... | ... | ... | ... | ... | ... |

### 高价值行示例

...

> 多个简单小 Sheet 可合并在同一子文档，但每个 Sheet 必须为独立块（用 `## Sheet:` 分隔）。

---

## Sheet: <SheetName3> — 功能域拆分示例

### 核心标识

| 列名 | 类型 | C/S | 语义 | 约束 | 默认值 | 依赖 |
|------|------|-----|------|------|--------|------|
| ID | ... | ... | ... | ... | ... | ... |
| GameplayClass | ... | ... | ... | ... | ... | ... |

### 进入条件

| 列名 | 类型 | C/S | 语义 | 约束 | 默认值 | 依赖 |
|------|------|-----|------|------|--------|------|
| OwnerCheckGrade | ... | ... | ... | ... | ... | ... |

> 复杂 Sheet 可在 Sheet 块内部按功能域分组，但列仍只属于该 Sheet，不能与其他 Sheet 的列混合。

---

# pipeline 文档模板（<name>.pipeline.designdata.md）

# <BookName> 数据管线

> summary: <≤150字简介，从 Excel 到运行时的完整转换链路>

## 三层管线总览

| 层级 | 产出 | 关键文件 | 说明 |
|------|------|----------|------|
| Layer 1: 转表 | `<全局表名>` | `ddtconfig.lua` → `<Dir>/<BookName>.txt` | Excel → txt → Lua 全局表 |
| Layer 2: 扩展 | <扩展产出，无则写"无"> | `*Ext.lua` / `*DynamicExt.lua` | <扩展说明> |
| Layer 3: 运行时 | `<Mgr.m_*>` | `<Mgr路径>` → `<Load方法>` | <转换说明> |

> 基础表是否回收：<是/否>。若回收注明 AfterDesignTablesLoaded.lua 中条目。

## Layer 1: 转表

### ddtconfig 注册
- **doc.Name**：`<BookName>`
- **doc.Dir**：`<Dir>`
- **FormulaParsers**：<有/无>
- **SubBooks**：<分表列表及 ID 分区；无则"无">

### 表头结构
- Sheet 名：`<Sheet名>`
- 列数：<N> 列
- C/S 标记分布：<cs 列 N 个, c 列 M 个, s 列 K 个>

### 类型解析要点
<仅列出本表特有的类型解析规则，通用规则引用 GUIDE-design-data-location.md §2.7。无特殊规则写"无特殊类型解析规则"。>

## Layer 2: 静态扩展

### 扩展产出

| 产出类型 | 产出名 | 来源文件 | 说明 |
|---------|--------|---------|------|
| 派生字段 | `v.__XXX` | `*Ext.lua` | 含义 |
| 反向索引 | `TableName` | `*Ext.lua` | 用途 |
| DTE 处理列 | `<列名>` | `*DynamicExt.lua` | 处理器类型及值变化 |

> 若无扩展，整节写"本表无 Layer 2 扩展"。

## Layer 3: 运行时转换

### Mgr 加载链

| 方法 | 消费表 | 产出 m_* 成员 | 关键转换逻辑 |
|------|--------|-------------|-------------|
| `_LoadXXX` | `<表名>` | `m_XXX` | <转换描述> |

### 值转换速查

| 列名 | Excel 原始值 | 运行时值 | 转换方式 | 转换位置 |
|------|-------------|---------|---------|---------|
| <列名> | <示例值> | <转换后值> | <转换方式> | `<文件>` → `<方法>` |

> 仅列出**运行时值 ≠ Excel 原值**的列。原值直传的列不列。

### 运行时数据流

#### 场景：<场景名称>

```
1. <步骤描述>
   → <端>: `<函数调用>`  [搜索: <关键字>]

2. <步骤描述>
   → <端>: `<函数调用>`  [搜索: <关键字>]

3. <步骤描述>
   → ...
```

> 选择 1-2 个核心场景。每步必须有代码定位。

---

# coderef 文档模板（<name>.coderef.designdata.md）

# <BookName> 代码引用

> summary: <≤150字简介，每列的代码引用点与行为映射>

## 代码消费点

### GAS（服务端）

| 文件 | 搜索关键字 | 消费方式 | 消费的列 |
|------|-----------|----------|---------|
| `<相对路径>` | `<关键字>` | <bddpairs遍历/按ID查表/按条件筛选> | <消费的列> |

### GAC（客户端）

| 文件 | 搜索关键字 | 消费方式 | 消费的列 |
|------|-----------|----------|---------|
| `<相对路径>` | `<关键字>` | <消费方式> | <消费的列> |

### Common（共享）

| 文件 | 搜索关键字 | 消费方式 | 消费的列 |
|------|-----------|----------|---------|
| `<相对路径>` | `<关键字>` | <消费方式> | <消费的列> |

### Master（主控）

| 文件 | 搜索关键字 | 消费方式 | 消费的列 |
|------|-----------|----------|---------|
| `<相对路径>` | `<关键字>` | <消费方式> | <消费的列> |

> 某端无消费点则写"无"。

## 列→代码引用点

| 列名 | 引用文件 | 引用方式 | 用途 |
|------|---------|---------|------|
| <列名> | `<相对路径>` → `<函数名>` | <按ID查表/值→分发/字符串解析/...> | <用途> |

> 每列一行。纯展示列（描述文本、名称等）可省略。

## 关键列→行为映射

| 列名 | 值 | 触发的代码行为 | 代码定位 |
|------|----|---------------|---------|
| <列名> | <值> | <行为描述> | `<相对路径>` → `<函数名>` |

> 仅列**值影响代码分支**的映射。纯数据透传不列。每个映射必须有代码定位。

## 异常信号

| 日志关键字 | 可能的配置错误 | 归属列 | 排查方向 |
|-----------|---------------|--------|---------|
| <关键字> | <配置错误> | <列名> | <排查方向> |

> 若暂无已知异常信号，写"暂无已知异常信号"。
