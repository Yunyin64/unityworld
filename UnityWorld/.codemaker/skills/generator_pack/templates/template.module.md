# <Module 标题>

> summary: <≤150字简介>
> svn_rev: <SVN修订版本号>

## 模块定位
- <该模块在系统中的定位>

## 对外使用（Public Surface）
- 对外接口1：`<InterfaceOrFunctionName>` 或 `无显式对外接口`
  - 作用：<一句话说明该接口做什么；没有可写”无”>
  - 使用前置：<调用前需要满足的条件；没有可写”无”>
  - 输入/输出：<关键参数与返回结果摘要；没有可写”无”>
  - 使用样例：`<最小调用示例或典型调用链关键字>` 或 `无`
- 对外接口2：`<InterfaceOrFunctionName>` 或 `无显式对外接口`
  - 作用：<一句话说明该接口做什么；没有可写”无”>
  - 使用前置：<调用前需要满足的条件；没有可写”无”>
  - 输入/输出：<关键参数与返回结果摘要；没有可写”无”>
  - 使用样例：`<最小调用示例或典型调用链关键字>` 或 `无`
- 对外配置1：`<ConfigFileOrRegistryName>` 或 `无显式对外配置`
  - 作用：<一句话说明该配置向外暴露什么能力/约束；没有可写”无”>
  - 配置前置：<加载、注册、生成或启用该配置前需要满足的条件；没有可写”无”>
  - 关键字段/取值：<关键字段、格式、枚举或约束摘要；没有可写”无”>
  - 配置定义路径：`<代码配置填相对路径如 program/game/xxx/xxx.lua；策划配置表填 BookName_SheetName 如 FestivalGame_FestivalTask，不加文件路径前缀>` 或 `无`
  - 使用样例：`<最小配置片段、典型加载链、典型搜索关键字>` 或 `无`
- 对外配置2：`<ConfigFileOrRegistryName>` 或 `无显式对外配置`
  - 作用：<一句话说明该配置向外暴露什么能力；没有可写”无”>
  - 配置前置：<前置条件；没有可写”无”>
  - 关键字段/取值：<关键字段、格式、枚举或约束摘要；没有可写”无”>
  - 配置定义路径：`<相对路径>` 或 `无`
  - 使用样例：`<最小配置片段、典型加载链、典型搜索关键字>` 或 `无`

## 配置路径汇总

| 配置项 | 定义路径 | 用途 |
|--------|---------|------|
| <配置项1> | `<代码配置填相对路径；策划配置表填 BookName_SheetName>` | <一句话说明> |
| <配置项2> | `<代码配置填相对路径；策划配置表填 BookName_SheetName>` | <一句话说明> |

> 本表让开发者一眼知道”去哪改”。新增配置时必须同步更新此表。策划配置表的定位方式见 `generator_pack/GUIDE-design-data-location.md`，禁止以 BookName_SheetName 作为文件路径做 Glob 搜索。

## 总体架构（One-screen）
- 核心运作原理：
  - <用 3~6 条说明主流程/核心组件协作，不展开实现细节>
- 组件与职责：
  - `<ComponentA>`：<职责>
  - `<ComponentB>`：<职责>
  - `<ComponentC>`：<职责>
- 失败/降级总览：
  - <失败路径或兜底机制一眼说明>

## 边界（In / Out）
- In：<该模块明确负责的范围>
- Out：<该模块明确不负责的范围>

## 横向辐射模块

| 引用模块 | 引用代码位置 | 简要说明 |
|---------|------------|---------|
| <module_name> | `<file> → <keyword>` | <一句话说明引用方式与目的> |

> 列出本模块直接依赖的其他模块（调用接口、读写状态、依赖配置、监听事件），不包括标准库/全局基础设施/同目录子文档。业务模块的业务域辐射面见 SPEC-business-module.md §4.5，此处不重复。

## 关键实现定位（文件 + 搜索关键字）

> 目标：开发者拿到这份清单，无需全项目 grep，能直接跳转到任意职责的实现。
> 组织方式：按职责分组，每组对应"总体架构"中的一个组件/职责。

### 初始化 / 启动入口
- `<relative/path/to/init_file>`
  - 搜索关键字：`<InitFunc>`、`<RegisterFunc>`

### 核心处理逻辑
- `<relative/path/to/core_file>`
  - 搜索关键字：`<CoreFunc>`、`<MainLoopFunc>`

### 配置 / 注册表加载
- `<relative/path/to/config_file>`
  - 搜索关键字：`<LoadConfigFunc>`、`<RegisterTableFunc>`

### RPC / 事件处理入口
- `<relative/path/to/rpc_handler_file>`
  - 搜索关键字：`<Gac2GasHandlerFunc>`、`<MsgHubListenFunc>`

### 关键数据结构定义
- `<relative/path/to/struct_file>`
  - 搜索关键字：`<StructName>`、`<ClassName>`

### 异常 / 降级处理
- `<relative/path/to/error_handler_file>`
  - 搜索关键字：`<ErrorHandlerFunc>`、`<FallbackFunc>`

## 诊断信号（关键日志 / 错误模式）

> 反查索引：手持一条日志关键字 → 找到它属于哪个 Flow 的哪一步 → 顺链路继续排查。
> 每条日志只在对应 Flow/Slice 文档中内联一次，此处汇总作为快速入口。

| 日志关键字 / 错误模式 | 归属 Flow / 位置 | 含义 | 接下来检查 |
|-------------------|----------------|------|----------|
| `<log_keyword_1>` | `<flow_name>.flow` → 步骤N | <该日志出现意味着什么> | <检查哪个字段/调用链/配置> |
| `<log_keyword_2>` | `<flow_name>.flow` → 步骤M | <该日志出现意味着什么> | <检查哪个字段/调用链/配置> |
| `<error_pattern>` | `<slice_name>.slice` → 字段写入点 | <错误模式的含义> | <检查哪些状态/依赖模块> |

> 若无任何关键日志（如纯计算模块），写"本模块无关键运行时日志"。
