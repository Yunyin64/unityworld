-- Amount Keyword
-- 弹药类型通用机制：
local Amount = {}

--function Amount.PreStart(card, ctx)
--end

--function Amount.Start(card, ctx)
--end

--function Amount.Tick(card, ctx)
--end

--function Amount.Contest(card, ctx)
--end

--function Amount.Apply(card, ctx)
--end

LuaMgr:RegisterKeyword("Amount", Amount)
